#include <cstdint>
#include <iostream>
#include "fraction.h"

#ifndef _SAMPLER_H
#define _SAMPLER_H

class Sampler {
public:
	//Constructor & Destructor num/den
	Sampler(int n);
	~Sampler(void) {}

private:
	n,
	i,
	array[i];
};


#endif
