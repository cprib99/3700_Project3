#include <cstdint>
#include <iostream>

#ifndef _SAMPLER_H
#define _SAMPLER_H

class Sampler {
public:
	//Constructor & Destructor num/den
	Sampler(int n);
	~Sampler(void) {}

private:
	n,
	i,
	array[i];
};


#endif
